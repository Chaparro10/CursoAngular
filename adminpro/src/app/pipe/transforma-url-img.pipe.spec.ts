import { TransformaUrlImgPipe } from './transforma-url-img.pipe';

describe('TransformaUrlImgPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformaUrlImgPipe();
    expect(pipe).toBeTruthy();
  });
});
